DATABASE_URL=
IOC_SCHEMA=
IOC_TABLE_NAME=
IOC_COL_SHA=sha256          
LOGS_TABLE_NAME=logs
# Alert thresholds
CPU_HIGH=90
RAM_RATIO_HIGH=0.8

# Optional IOC seeding is disabled because you already have your own table
DISABLE_IOC_SEED=true

# Agent settings 
HOST_SCAN_PATH=/tmp
METRICS_INTERVAL=5
HASH_ENABLE=true
HASH_DIRS=C:\learn py
HASH_INTERVAL=3600
AGENT_MAX_SIZE_MB=1000
AGENT_MAX_FILES=20000
AGENT_WORKERS=4

# Email settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=barak.panker@gmail.com
SMTP_PASS=
ALERT_EMAILS=barak.panker@gmail.com   